var $assertion = $assertion || require('giant-assertion', '$assertion'),
    $data = $data || require('giant-data', '$data'),
    $oop = $oop || require('giant-oop', '$oop'),
    $utils = $utils || require('giant-utils', '$utils'),
    Q = Q || require('q', 'Q');

/**
 * TODO: Rename when giant-asset is no longer used.
 * @namespace
 */
var $asset2 = {};
